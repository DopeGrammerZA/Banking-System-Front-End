apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  labels:
    app: banking-mongodb
spec:
  type: ClusterIP
  selector:
    app: banking-mongodb
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017